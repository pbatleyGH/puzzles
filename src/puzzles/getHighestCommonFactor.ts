/**
 * Get highest common factor
 */
export function getHighestCommonFactor(input: number[]) {}

function getPrimeFactors(number: number) {
  if (number < 1) {
    throw Error("Cannot factorise negative integer");
  }
}
